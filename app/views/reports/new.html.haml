%div{ "data-role" => "content"}
  %div{ "class" => "ui-body ui-body-d"}
    %h2 Report a Problem
    %p Notice something incorrect? Wrong price? Something not marked as gluten free? Let us know!
    = form_for @report do |f|
      -if @report.errors.any?
        #error_explanation
          %h2= "#{pluralize(@report.errors.count, "error")} prohibited this dish from being saved:"
          %ul
            - @report.errors.full_messages.each do |msg|
              %li= msg

      .field
        = f.label :description
        = f.text_area :description
        
      .actions
        = f.submit 'Save'
